{
  "name": "cache-vcpkg",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "ncc build src/restore.js -o dist/restore && ncc build src/save.js -o dist/save",
    "test": "jest"
  },
  "dependencies": {
    "@actions/cache": "^4.0.3",
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "jest": "^29.7.0"
  }
}
